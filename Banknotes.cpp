#include<bits/stdc++.h>
using namespace std;
int main(){
    cin.sync_with_stdio(false);
    cout.sync_with_stdio(false);
    int t;
    cin>>t;
    while(t--){
        int n;
        long k;
        cin>>n>>k;
        long long total=0;
        int numberOfDigit[10];
        for(int i=0;i<n;i++){
            cin>>numberOfDigit[i]
        }
        k++;
        while(){
            long long num=pow(10,numberOfDigit);

        }
    }

}
